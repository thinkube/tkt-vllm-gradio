apiVersion: thinkube.io/v1
kind: TemplateManifest
metadata:
  name: tkt-vllm-gradio
  title: vLLM Inference Server
  description: High-performance LLM inference with vLLM engine — model managed by LLM Gateway
  version: 1.0.0
  author: Alejandro Martínez Corriá
  tags: ["ai", "llm", "vllm", "inference", "text-generation", "gpu"]

parameters: []

secrets:
  - name: HF_TOKEN
    description: Hugging Face API token — required at runtime for gated models (Llama, Mistral, etc.)
    required: false

